<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guidance Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-pFh6lIRH6jQ3+1I1Hs3ey4V5E0vjH0GkAG9JMEgJ9HkWQvhj+YrE5zLX+tVUQSxZw36r/SI5K5AucA1Q++oWyg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f9fc;
            padding-top: 50px;
        }

        .container {
            max-width: 900px;
            margin: auto;
        }

        h1 {
            text-align: center;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 40px;
        }

        .guidance {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
            text-align: center;
        }

        .guidance h2 {
            font-size: 1.5rem;
            color: #007bff;
        }

        .guidance p {
            font-size: 1.1rem;
            color: #2c3e50;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Your Guidance</h1>
        <div class="guidance">
            <h2 id="guidanceTitle"></h2>
            <p id="guidanceText"></p>
        </div>
    </div>

    <script>
        // Get the score from the server-side variable passed by Express
        const scores = '<%= score %>';

        const guidanceTitle = document.getElementById("guidanceTitle");
        const guidanceText = document.getElementById("guidanceText");

        // Provide guidance based on the score
        if (scores <= 10) {
            guidanceTitle.textContent = "Low Risk";
            guidanceText.textContent = "You are at low risk for addiction. Maintain a healthy lifestyle and avoid triggers.";
        } else if (scores <= 20) {
            guidanceTitle.textContent = "Moderate Risk";
            guidanceText.textContent = "You are at moderate risk for addiction. Consider seeking early intervention techniques such as counseling or support groups.";
        } else {
            guidanceTitle.textContent = "High Risk";
            guidanceText.textContent = "You are at high risk for addiction. It's important to seek professional help and consider rehabilitation services.";
        }
    </script>
</body>

</html>
