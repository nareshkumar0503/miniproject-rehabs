<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Appointment</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .appointment-card {
            margin-top: 50px;
            padding: 30px;
            background-color: #f8f9fa;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        .appointment-header {
            background-color: #5cb85c;
            color: white;
            padding: 20px;
            border-radius: 10px 10px 0 0;
        }

        .appointment-header h3 {
            margin: 0;
        }

        .appointment-body {
            padding: 20px;
        }

        .appointment-info {
            margin-bottom: 20px;
        }

        .appointment-info label {
            font-weight: bold;
            color: #6c757d;
        }

        .btn-view-more {
            background-color: #007bff;
            color: white;
        }

        .text-center {
            text-align: center;
            padding: 50px;
            margin-top: 50px;
        }

        .text-center h4 {
            font-size: 1.8rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
        }

        .text-center p {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 25px;
            line-height: 1.6;
        }

        .btn-primary {
            display: inline-block;
            padding: 12px 25px;
            font-size: 1rem;
            font-weight: bold;
            text-transform: uppercase;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            text-decoration: none;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .btn-primary:focus {
            outline: none;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
        }

        /* cancle button */
        .btn-danger {
            background-color: #e74c3c;
            border: none;
        }

        .btn-warning {
            background-color: #f39c12;
            border: none;
            margin-right: 10px;
        }

        .btn-secondary {
            background-color: #95a5a6;
            border: none;
        }

        .confirmation-box {
            margin-top: 20px;
            padding: 15px;
            border: 1px solid #ddd;
            background-color: #f7f7f7;
            border-radius: 5px;
            text-align: center;
        }

        .confirmation-box p {
            font-size: 16px;
            margin-bottom: 10px;
            color: #333;
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <% if (patient) { %>
                    <div class="appointment-card">
                        <div class="appointment-header text-center">
                            <h3>Your Appointment</h3>
                        </div>
                        <div class="appointment-body">
                            <div class="appointment-info">
                                <label>Token No:</label>
                                <p>
                                    <% if (patient.tokenNo) { %>
                                        <%= patient.tokenNo %>
                                            <% } else { %>
                                                Yet to be assigned
                                                <% } %>
                                </p>
                            </div>
                            <div class="appointment-info">
                                <label>Center Name:</label>
                                <p>
                                    <% if (patient.centerName) { %>
                                        <%= patient.centerName %>
                                            <% } else { %>
                                                Yet to be assigned
                                                <% } %>
                                </p>
                            </div>
                            <div class="appointment-info">
                                <label>Time:</label>
                                <p>
                                    <% if (patient.time) { %>
                                        <%= patient.time %>
                                            <% } else { %>
                                                Yet to be assigned
                                                <% } %>
                                </p>
                            </div>
                            <div class="appointment-info">
                                <label>Condition (Addiction):</label>
                                <p>
                                    <%= patient.patientAddiction %>
                                </p>
                            </div>
                            <div class="appointment-info">
                                <label>Appointment Date:</label>
                                <p>
                                    <%= patient.date %>
                                </p>
                            </div>
                            <div class="appointment-info">
                                <label>Session:</label>
                                <p>
                                    <%= patient.appointmentSession %>
                                </p>
                            </div>
                            <div class="appointment-info">
                                <label>Center Email:</label>
                                <p>
                                    <%= patient.centerEmail %>
                                </p>
                            </div>
                            <div class="appointment-info">
                                <label>Status:</label>
                                <p class="<%= patient.status === 'Confirmed' ? 'text-success' : 'text-warning' %>">
                                    <%= patient.status %>
                                </p>
                            </div>
                            <!-- Cancel Appointment Button -->
                            <button class="btn btn-danger btn-block" id="cancelAppointmentBtn">Cancel
                                Appointment</button>

                            <!-- Confirmation Section (Initially hidden) -->
                            <div id="cancelConfirmation" class="confirmation-box" style="display: none;">
                                <p>Are you sure you want to cancel your appointment?</p>
                                <button class="btn btn-warning" id="confirmCancelBtn">Yes, Cancel</button>
                                <button class="btn btn-secondary" id="dismissCancelBtn">No, Keep Appointment</button>
                            </div>
                        </div>
                    </div>
                    <% } else { %>
                        <div class="text-center">
                            <h4>No appointment found for your account.</h4>
                            <p>It seems like you haven't booked an appointment yet. Please click below to schedule one.
                            </p>
                            <a href="/" class="btn btn-primary">Explore Centers and Schedule Your Appointment</a>
                        </div>
                        <% } %>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Get the cancel button and confirmation section
        const cancelAppointmentBtn = document.getElementById('cancelAppointmentBtn');
        const cancelConfirmation = document.getElementById('cancelConfirmation');
        const confirmCancelBtn = document.getElementById('confirmCancelBtn');
        const dismissCancelBtn = document.getElementById('dismissCancelBtn');

        // Show the confirmation section when 'Cancel Appointment' is clicked
        cancelAppointmentBtn.addEventListener('click', () => {
            cancelConfirmation.style.display = 'block';
            cancelAppointmentBtn.style.display = 'none';
        });

        // Handle 'Yes, Cancel' button click
        confirmCancelBtn.addEventListener('click', () => {
            // Here, you can trigger the actual cancellation logic (e.g., sending a request to the server)
            alert('Appointment canceled successfully!');
            // Optionally, you can redirect or hide the confirmation section
        });

        // Handle 'No, Keep Appointment' button click
        dismissCancelBtn.addEventListener('click', () => {
            cancelConfirmation.style.display = 'none';
            cancelAppointmentBtn.style.display = 'block';
        });
    </script>
</body>

</html>