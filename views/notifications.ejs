<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Notification Toggle</title>
  <style>
    *,
    *::before,
    *::after {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --primary: #0676ed;
      --background: #222b45;
      --success: #12c99b;
      --error: #e41749;
    }

    .notification-section  {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      gap: 20px;
      background-color: var(--background);
    }

    .notification-section .alert-container {
      z-index: 3000;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .notification-section .toggle-button {
      margin: 10px;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
      color: #fff;
      background-color: var(--primary);
      transition: background-color 0.3s;
    }

    .notification-section .toggle-button:hover {
      background-color: #045bb5;
      /* Darker color */
    }

    .notification-section .alert {
      min-height: 67px;
      max-width: 90%;
      border-radius: 12px;
      padding: 16px 22px;
      display: none;
      /* Hidden by default */
      position: fixed;
      /* Fixed to the screen */
      top: 20px;
      /* Distance from top */
      right: 20px;
      /* Distance from right */
      color: #fff;
      font-family: Verdana;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3);
      display: flex;
      align-items: center;
      /* Prevent text from wrapping */
      z-index: 3000;
      word-wrap: break-word; /* Ensure long words break into new lines */
      word-break: break-all; /* Break the word if it's too long */
      white-space: normal; /* Allow text to wrap normally */
    }

    .notification-section .alert-success {
      background: var(--success);
    }

    .notification-section .alert-error {
      background: var(--error);
    }

    .notification-section .alert p {
      margin-left: 10px;
      margin-right: 10px;
      flex: 1;
    }
    .notification-section .close {
      font-size: 18px;
      cursor: pointer;
      margin-left: 10px;
      flex-shrink: 0;
      /* Prevent close button from shrinking */
    }

    .notification-section .close:hover {
      transform: scale(1.3);
    }
  </style>
</head>

<body>
  <div class="notification-section">
  <div class="alert-container">

    <div id="notification" class="alert" style="display: none;">
      <p id="message"></p>
      <span id="close" class="close">×</span>
    </div>
  </div>
</div>
<script src="./js/patientRegister.js"></script>
</body>

</html>